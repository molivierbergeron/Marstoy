node_modules/
.wrangler/
# Secrets locaux (clé Rebrickable) — ne doit jamais partir dans git.
.dev.vars
.dev.vars.*
!.dev.vars.example
.env
.DS_Store

# Rapport de reconnaissance : produit par le workflow (qui le force-add),
# pas par les exécutions locales.
data/recon.json
