{
  "name": "marstoy-real",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Proxy Cloudflare Worker qui affiche marstoy.com avec les vrais noms et visuels des sets LEGO.",
  "scripts": {
    "check": "node scripts/check.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "node --test 'test/*.test.js'"
  },
  "devDependencies": {
    "esbuild": "^0.25.12",
    "wrangler": "^4.20.0"
  }
}
